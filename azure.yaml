# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-functions-eventhub-sdk-bindings-python
services:
  eventhub:
    project: ./eventhub_samples_eventdata
    language: python
    host: function